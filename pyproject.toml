[project]
name = "record-management-system"
version = "0.1.0"
description = "Tkinter record management system for a specialist travel agent"
requires-python = ">=3.10"
dependencies = [
    "Flask>=3.0",
]

[tool.unittest]
testpaths = ["tests"]

